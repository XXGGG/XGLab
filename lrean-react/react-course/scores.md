# React 学习打卡评分

> 每完成一天的课程，我会在这里记录你的完成情况和评分。

## 评分说明

- 完成度：是否完成当天课程要求的内容。
- 代码质量：结构是否清晰、命名是否合理、是否符合 React 思路。
- 主动探索：是否有做超出要求的尝试。
- 评分范围：1–5 分（5 分为满分，对应 ⭐ 的个数）。

---

## Day 1：React 项目结构 & JSX 入门 🎯

- 完成度：⭐⭐⭐⭐⭐  
  - 要求的内容都实现了：标题、昵称、已有技能、在学课程，页面正常渲染，结构清晰。  
- 代码质量：⭐⭐⭐⭐  
  - 优点：
    - 变量命名直观：`name`、`skills`、`courses` 一看就知道含义。  
    - 使用数组保存技能/课程，后续很容易改成循环渲染。  
    - 使用 `className="app-container"`，已经有组件样式意识。  
  - 可以改进的小点：
    - `<title>{title}</title>` 一般不放在组件 JSX 里，而是放在 `index.html` 或后面用专门的库（例如 react-helmet）管理页面标题，现在这样写虽然能运行，但不是常见写法。  
    - `title` 变量里已经包含书名号，可以在 JSX 中直接写普通字符串，比如 `const title = 'XGLab React 学习首页'`，然后在 JSX 里决定是否加引号或书名号，会更灵活。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 不仅完成了基础要求，还额外加了「正在学习的课程」这一块，并且提前把你想学的路线写进代码里，学习目标很清晰。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day1 完成度非常好，结构简洁，没有多余的逻辑，也没有明显坏习惯。接下来只要按照课程继续往下推进，并把每天的内容拆成独立页面，就能很自然地过渡到多页面和路由。  

---

## Day 2：组件状态 & 事件处理 ⚙️

- 完成度：⭐⭐⭐⭐⭐  
  - `Counter` 和 `TextMirror` 两个组件都已按要求实现，并额外完成了「重置」按钮和输入状态提示逻辑。  
  - 创建了 `pages/Day2StateAndEvents.tsx` 页面组件，并在 `App.tsx` 中只渲染该页面，和课程思路完全一致。  
- 代码质量：⭐⭐⭐⭐⭐  
  - `Counter`：
    - 使用 `useState<number>(0)` 明确了类型，逻辑简单清晰。  
    - 抽出 `handleClick` 和 `reset` 两个函数，语义明确。  
  - `TextMirror`：
    - 使用 `useState<string>('')` 和 `React.ChangeEvent<HTMLInputElement>`，TypeScript 使用得很标准。  
    - 文本显示部分用三元运算符根据长度显示「请开始输入」或实际内容，逻辑紧凑。  
  - 页面组件 `Day2StateAndEvents`：
    - 清晰地分出了两个 section，各自有标题和对应组件，结构一目了然。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 按 Day2 的扩展要求完成了「重置」和「空输入提示」，并已经开始用 `pages` 目录来组织页面，为后续路由打下基础，这种「提前考虑结构」的习惯非常好。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day2 不只是完成了功能，而且在组织代码结构和 TS 类型使用上都非常到位，已经有初级工程化思维。继续这样积累，后面引入路由和更多页面会非常顺滑。  

---

## Day 3：列表渲染 & 条件渲染 📋

- 完成度：⭐⭐⭐⭐⭐  
  - `learnedSkills` / `learningCourses` 都使用 `map` 正确渲染成列表。  
  - 三块区域（技能、课程、待办事项）都实现了条件渲染逻辑。  
  - Todo 区域包含 id/title/finished 字段，覆盖了作业要求。  
- 代码质量：⭐⭐⭐⭐⭐  
  - 使用 `type Course` / `type Todo` 明确定义数据结构，TS 使用规范。  
  - 判断是否有数据用 `const hasXxx = arr.length > 0`，再在 JSX 中使用，逻辑清晰。  
  - 条件渲染结构简洁，`? :` 和 `&&` 的应用都很自然，没有多余嵌套。  
  - 列表 key 选择合理：技能用字符串本身，课程用 `course.name`，todo 用 `id`，是实际项目中推荐的写法。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 在 Todo 区域中使用「已完成 / 未完成」文案，已经具备「状态 + 展示」的意识。  
  - Day3 页面整体像一个小型 dashboard，为后面加交互（比如勾选完成）预留了空间。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day3 已经基本掌握列表渲染和条件渲染这两个核心能力，这在真实项目中非常常用。你的代码结构清晰、类型定义规范，下一步可以在此基础上继续练习交互（增删改查）和路由。  

---

## Day 4：组件拆分 & props 🧩

- 完成度：⭐⭐⭐⭐⭐  
  - 已将 `Counter`、`TextMirror` 移动到 `src/components` 目录。  
  - 新增 `LessonCard`、`SectionTitle`、`EmptyState` 等组件，并在 Day3 页面中使用。  
- 代码质量：⭐⭐⭐⭐⭐  
  - 所有组件都为 props 明确声明类型（`LessonCardProps`、`SectionTitleProps`、`EmptyStateProps`），TS 使用规范。  
  - `SectionTitle` 抽离了标题展示，并配有独立样式文件 `SectionTitle.css`，结构清晰。  
  - Day3 页面顶部组合使用 `LessonCard + SectionTitle + EmptyState`，页面层只负责组织结构，小组件各司其职，符合组件化思想。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 不仅完成了课程要求的组件拆分，还额外实现了 `EmptyState` 这种通用空状态组件，在多个 section 中复用，已经有「设计组件积木」的意识。  
  - 组件命名直观、职责单一，后续在其他页面中继续复用没有心理负担。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day4 之后，你的项目已经从「练习文件堆」升级成「有 pages 和 components 分层的小型应用」。后面接入路由、继续扩展页面都会非常自然，你已经具备了初级组件设计能力。  

---

## Day 5：路由与多页面应用 🛣️

- 完成度：⭐⭐⭐⭐⭐  
  - 成功安装并配置了 `react-router-dom`。  
  - `App.tsx` 正确使用了 `<Routes>` 和 `<Route>` 定义路由表。  
  - `main.tsx` 正确包裹了 `<BrowserRouter>`，这是很多初学者容易遗漏的，你一次就做对了。  
  - 成功迁移了 Day1-Day3 的内容到 `src/pages` 目录，并保持了之前的作业记录。  
- 代码质量：⭐⭐⭐⭐⭐  
  - **导航栏实现优秀**：使用了 `NavLink` 并利用 `isActive` 属性来实现「当前页高亮」样式，这是 React Router 的最佳实践。  
  - **首页设计合理**：`Home.tsx` 使用数组 + `map` 渲染课程列表，而不是写死的 `Link`，以后加 Day6、Day7 只需要改数组数据，非常易于维护。  
  - **目录结构清晰**：`src/pages` 和 `src/components` 分工明确，页面组件和通用组件分开了，项目结构非常标准。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 在 `Home.tsx` 中使用了动态列表渲染来生成导航链接，体现了你对 Day3「列表渲染」知识的活学活用。  
  - 页面迁移过程中没有破坏原有功能，Day2 和 Day3 的逻辑依然完整保留。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day5 是一个里程碑。从此之后，你的应用不再是「单页练习板」，而是一个真正的「多页面 Web 应用」。你已经掌握了 React 开发中最核心的骨架搭建能力（组件 + 路由）。接下来的课程，我们可以专注于在这个骨架上填充更复杂的交互逻辑了。  

---

## Day 6：组件通信与数据流 🔁

- 完成度：⭐⭐⭐⭐⭐  
  - 按要求实现了 `UserProfile` 展示组件和 `EditProfile` 编辑组件。  
  - 在 Day6 页面中完成了状态提升，确保预览区和编辑区始终使用同一份 `user` 数据。  
  - 在 `App.tsx` 中配置了 `/day6` 路由，并在导航栏添加了 Day6 入口。  
- 代码质量：⭐⭐⭐⭐⭐  
  - 使用独立的 `User` 类型，并在多个组件间复用，类型设计清晰。  
  - `EditProfile` 正确通过 props 接收 `user` 和 `onSave` 回调，内部状态与父组件状态配合良好。  
  - `Day6DataFlow` 中通过 `useState<User>` 管理单一数据源，再把数据和更新函数同时下发到子组件，完全符合 React 单向数据流的推荐写法。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 预览区域和编辑区域的双栏布局让数据流的方向一目了然，交互体验很好。  
  - 为用户信息添加了头像 URL，并在更新时通过展开运算符保留其余字段，体现了你对数据结构整体性的思考。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day6 之后，你已经真正掌握了「父组件集中管理状态 + 子组件通过回调上报变化」这一模式，这是中大型 React 应用中非常常见的写法。接下来你可以在这个基础上继续挑战更复杂的表单、列表编辑和状态管理场景。  

---

## Day 7：可操作的 Todo 列表 ✅

- 完成度：⭐⭐⭐⭐⭐  
  - 成功按照课程要求新建了 `Day7TodoList` 页面，并接入了 `/day7` 路由。  
  - 列表支持「添加 / 切换完成状态 / 删除」三种操作，逻辑完整闭环。  
- 代码质量：⭐⭐⭐⭐⭐  
  - 使用 `initialTodos` 作为初始数据，再用 `useState` 管理 `todos`，结构清晰。  
  - `TodoItem` 独立成组件，并通过 props 接收 `title`、`finished` 和两个回调，页面组件只负责管理列表状态，非常符合「父管状态、子管展示」的思路。  
  - 使用 `setTodos([...todos, newTodo])`、`filter` 和 `map` 做不可变更新，没有直接修改原数组，这是 React 中操作列表的标准写法。  
  - 在列表上方增加了「共 X 项，已完成 Y 项」的统计信息，体验更完整。  
- 主动探索：⭐⭐⭐⭐⭐  
  - 在 `TodoItem` 中用 `textDecoration: finished ? 'line-through' : 'none'` 为已完成的待办加上删除线，让状态变化一眼就能看出来。  
  - 在 `App.tsx` 中把路由抽成 `pathlist` 数组，再统一渲染导航和 `<Route>` 列表，这已经是小型项目常用的「路由配置表」写法。  
- 综合评价：⭐⭐⭐⭐⭐  
  - Day7 这一关，你不仅做出了一个可操作的 Todo 列表，还顺便体验了不可变更新、组件抽离、路由配置抽象等一整套更进阶的实践。到这里，你已经可以自己设计一个简单的小功能页面，包括：数据结构、交互逻辑、组件划分和路由接入，完全配得上「React 入门阶段的阶段性小结」。  

