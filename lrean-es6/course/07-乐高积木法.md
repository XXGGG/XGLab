# 07-乐高积木法.md

📦 欢迎来到第七课！

小时候玩过乐高吗？一个巨大的城堡是由成千上万个小积木拼起来的。
现代前端开发（Nuxt/Next）也是一样，我们不会把所有代码写在一个文件里（那会死人的），而是拆成一个个小文件（模块），然后拼装起来。

这就是 **模块化 (Modules)**：`import` 和 `export`。

## 🐣 基础用法

### 1. `export`：我要把这个东西分享出去

你可以把变量、函数、类导出去给别人用。

**方式一：命名导出 (Named Export)**
就像超市货架上的商品，每个都有名字。

```javascript
// math.js
export const pi = 3.14;
export const add = (a, b) => a + b;
```

**方式二：默认导出 (Default Export)**
每个文件只能有一个“镇店之宝”。

```javascript
// User.js
const User = { name: "Trae" };
export default User; 
```

### 2. `import`：我要把那个东西拿进来用

**导入命名导出**（名字必须对上，要用 `{}` 包起来）：
```javascript
import { pi, add } from './math.js';
console.log(pi); // 3.14
```

**导入默认导出**（名字随便起，不需要 `{}`）：
```javascript
import MyUser from './User.js'; // 我爱叫啥叫啥
console.log(MyUser); 
```

## 🎭 场景实战

### Vue (Nuxt) 中的用法

Nuxt 有个超能力叫“自动导入”，很多时候你甚至不需要写 `import`。但在普通 Vue 文件里：

```javascript
<script setup>
// 导入 Vue 的功能
import { ref, onMounted } from 'vue'; 

// 导入组件
import Button from './components/Button.vue';

// 导入工具函数
import { formatDate } from './utils/date.js';
</script>
```

### React (Next) 中的用法

React 开发就是不断的 Import 和 Export 组件。

```javascript
// components/Header.js
export default function Header() {
  return <header>Logo</header>;
}

// pages/index.js
import Header from '../components/Header'; // 导入组件
import { useState } from 'react'; // 导入 React Hook

export default function Home() {
  return (
    <div>
      <Header />
      <h1>Welcome</h1>
    </div>
  );
}
```

## 🧠 总结

- `export` 是卖东西，`import` 是买东西。
- `export default` 是专卖店（只卖一样），`import` 时不需要花括号。
- `export const` 是百货店（卖很多样），`import` 时需要花括号 `{}` 挑选。
- 写组件的时候，通常一个文件写一个组件，用 `export default` 导出。

## ✍️ 课后作业

1.  假设有一个 `utils.js` 文件，里面导出了 `export const A = 1;` 和 `export default 2;`。
    请写出导入这两个值的代码，分别命名为 `valA` 和 `valDefault`。

---
**下一课预告**：代码执行顺序有时候不听话？先干完这个再干那个？教你优雅地“等外卖”。🍕


## 🟢 XXG作业：

```javascript
import { valA : A } from './utils.js'
import valDefault from './utils.js'
```