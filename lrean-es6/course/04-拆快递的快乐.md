# 04-拆快递的快乐.md

🎁 欢迎来到第四课！

你有没有这种经历：买了一堆快递，然后一个个把它们拆开，拿出里面的东西？
在代码里，**解构赋值 (Destructuring)** 就是这种感觉。它能让你从复杂的对象或数组里，直接把想要的数据“提取”出来。

## 🐣 基础用法

### 1. 对象解构：精准拿取

假设你有一个用户对象：

```javascript
const user = {
  name: "Trae",
  age: 18,
  role: "Teacher",
  city: "New York"
};
```

以前你想拿名字和年龄：
```javascript
const name = user.name;
const age = user.age;
// 写多了好烦啊！😫
```

现在：
```javascript
const { name, age } = user;
// 搞定！变量名必须和属性名一样哦。
console.log(name, age); // "Trae", 18
```

**改名卡**：如果不想用原来的名字？
```javascript
const { name: userName } = user;
console.log(userName); // "Trae"
```

### 2. 数组解构：按顺序拿

数组没有属性名，只有顺序。

```javascript
const colors = ["Red", "Green", "Blue"];

const [first, second] = colors;
console.log(first); // "Red"
console.log(second); // "Green"
```

### 3. 默认值：防止只有空气

万一盒子里没东西咋办？我们可以设个保底。

```javascript
const { level = "L1" } = user; 
// user 里没有 level，所以 level 变量就是 "L1"
```

## 🎭 场景实战

### Vue (Nuxt) 中的用法

在 Vue 3 的 API 里，解构无处不在。

```javascript
// 从 vue 包里解构出我们需要的方法
import { ref, onMounted } from 'vue';

// 假设 props 是 { title: 'Hi', show: true }
const props = defineProps(['title', 'show']);
// 注意：直接解构 props 会失去响应性！但在 script setup 里通常直接用 props.title
// 如果需要解构且保持响应性，可以用 toRefs
import { toRefs } from 'vue';
const { title } = toRefs(props);
```

### React (Next) 中的用法

React 最爱解构了，尤其是组件的 Props。

```javascript
// 以前
const UserCard = (props) => {
  return <div>{props.name} - {props.age}</div>;
};

// 现在：直接在参数里解构！爽！
const UserCard = ({ name, age, role = "Guest" }) => {
  return <div>{name} - {age} ({role})</div>;
};
```

Hooks 也是数组解构的典型：
```javascript
const [count, setCount] = useState(0);
// useState 返回一个数组，第一个是值，第二个是设置函数
```

## 🧠 总结

- 看到 `const { a, b } = obj` 别懵，它就是 `const a = obj.a; const b = obj.b`。
- 函数参数里看到 `{ name }`，那是直接把传进来的对象拆开了。
- 能解构就解构，代码少写一半，快乐多一倍。

## ✍️ 课后作业

1.  给定对象 `const book = { title: "ES6 Guide", author: "Trae", price: 99 };`，请用一行代码取出 `title` 和 `price`。
2.  给定数组 `const sizes = ["S", "M", "L", "XL"];`，请用解构取出 "S" 赋值给 `small`，取出 "L" 赋值给 `large` (提示：中间的可以用逗号跳过)。

---
**下一课预告**：学会了拆快递，那怎么把一堆东西打包或者复制一份呢？我们要学“影分身之术”！👯


## 🟢 XXG作业：

```javascript
const book = { title: "ES6 Guide", author: "Trae", price: 99 };
const { title, price } = book

const sizes = ["S", "M", "L", "XL"];
const { small, ,large } = sizes
```

