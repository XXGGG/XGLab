# 06-循环杀手.md

🔄 欢迎来到第六课！

你是怎么遍历数组的？
`for (var i = 0; i < arr.length; i++)`？
如果是，请把这段代码刻在墓碑上，因为我们要埋葬它了。⚰️

ES6 提供了两个超好用的数组方法：`map` 和 `filter`。它们让数据处理变得像说话一样自然。

## 🐣 基础用法

### 1. `map`：加工厂

`map` 的意思是“映射”。它会遍历数组的**每一个元素**，把它拿出来加工一下，然后**返回一个新的数组**。

**例子**：把所有数字乘以 2。

```javascript
const nums = [1, 2, 3];

// item 就是当前的元素
const doubled = nums.map(item => item * 2);

console.log(doubled); // [2, 4, 6]
// 原数组 nums 没变哦！
```

### 2. `filter`：过滤器

`filter` 顾名思义，就是过滤。它会遍历数组，**留下符合条件的**，扔掉不符合的。

**例子**：只保留及格的分数（>= 60）。

```javascript
const scores = [55, 80, 92, 40];

// 返回 true 就保留，false 就扔掉
const passed = scores.filter(score => score >= 60);

console.log(passed); // [80, 92]
```

## 🎭 场景实战

### Vue (Nuxt) 中的用法

Vue 的 `v-for` 指令其实就是模板里的 `map`。但在 `<script>` 里处理数据时，我们大量使用它们。

```javascript
<script setup>
const products = ref([
  { name: 'Apple', price: 10 },
  { name: 'Banana', price: 5 }
]);

// 计算所有商品打折后的价格列表
const salePrices = computed(() => {
  return products.value.map(p => {
    return { ...p, price: p.price * 0.8 }; // 打八折
  });
});
</script>
```

### React (Next) 中的用法

React 没有 `v-for`，我们**直接在 JSX 里用 `map`** 来渲染列表。这是 React 开发者的肌肉记忆！

```javascript
const UserList = ({ users }) => {
  return (
    <ul>
      {/* 把数据数组 map 成元素数组 */}
      {users.map(user => (
        <li key={user.id}>
          {user.name}
        </li>
      ))}
    </ul>
  );
};
```

删除列表中的某一项（用 `filter`）：
```javascript
const deleteUser = (id) => {
  // 过滤掉 id 匹配的那个，保留剩下的 -> 变相等于删除了
  setUsers(users.filter(user => user.id !== id));
};
```

## 🧠 总结

- 想要**修改**数组里的每一项并生成新数组？用 `map`。
- 想要**筛选**数组里的部分项？用 `filter`。
- 想要**查找**某一个？用 `find` (课外了解)。
- 别再写 `for` 循环了，除非你真的很想念 `i++`。

## ✍️ 课后作业

1.  给定数组 `const prices = [10, 20, 30];`，请用 `map` 生成一个新数组，每个价格前面加上 "¥" 符号（结果：`["¥10", "¥20", "¥30"]`）。
2.  给定数组 `const ages = [12, 18, 25, 8, 30];`，请用 `filter` 找出所有已成年（>=18）的年龄。

---
**下一课预告**：代码写多了，文件越来越长怎么办？像搭乐高一样把代码拆开！📦

## 🟢 XXG作业：
```javascript
const prices = [10, 20, 30];
const newPrices = prices.map(p => `¥${p}`)

const ages = [12, 18, 25, 8, 30];
const newAges = ages.filter(a => a > 18)
```