# 06-联合类型与类型别名 🤝

## 👋 嗨！
有时候，世界不是非黑即白的。一个变量可能今天是数字，明天就变成了字符串。
比如商品的 ID，有的系统是数字 `1001`，有的系统是字符串 `"PROD_1001"`。

TS 允许这种情况存在，这就是 **联合类型 (Union Types)**。

---

## 🛤️ 1. 联合类型：是这个，或者那个

用竖线 `|` 分隔不同的类型。

```typescript
let id: string | number;

id = 101;       // ✅
id = "202-A";   // ✅
// id = true;   // ❌ 报错，只能是 string 或 number
```

### ⚠️ 小心！
当你使用联合类型时，TS 只能保证你能访问**共有**的方法。

```typescript
function printId(id: string | number) {
  // console.log(id.length); // ❌ 报错！因为 number 没有 length 属性
  
  // ✅ 正确做法：先判断类型 (类型守卫)
  if (typeof id === "string") {
    console.log("String ID length:", id.length); // 这里 TS 知道 id 是 string
  } else {
    console.log("Number ID:", id); // 这里 TS 知道 id 是 number
  }
}
```

---

## 🏷️ 2. 类型别名 (Type Alias)

如果 `string | number` 这种写法你要写几十遍，是不是很烦？
我们可以给它起个名字，这就叫 **类型别名**，用 `type` 关键字。

```typescript
// 给 string | number 起个外号叫 ID
type ID = string | number;

let userId: ID = 123;
let orderId: ID = "Order-456";

function findUser(id: ID) {
  // ...
}
```

### Type vs Interface
这俩经常让人晕。简单区分：
- **Interface**: 专门用来描述**对象**的形状（支持继承、合并）。
- **Type**: 也就是个**别名**。它可以描述对象，也可以描述联合类型、基础类型等。

*小白准则：描述对象优先用 Interface，描述“或”关系或者给基础类型起名用 Type。*

---

## 🍬 3. 字面量类型 (Literal Types)

这是 TS 里超级好用的一个特性！
你可以规定一个变量**只能是某几个特定的值**。

```typescript
// gender 只能是 "male" 或 "female"，其他字符串不行
type Gender = "male" | "female";

let myGender: Gender = "male";
// myGender = "unknown"; // ❌ 报错

// 配合联合类型，做状态管理神器
type Status = "loading" | "success" | "error";

let requestStatus: Status = "loading";
```

---

## 🖼️ Vue 和 React 场景

### Vue (Props 类型)
```typescript
// 只有这三种大小时才有效
type ButtonSize = 'small' | 'medium' | 'large';

defineProps<{
  size: ButtonSize;
  id: string | number;
}>();
```

### React (组件属性)
```tsx
type ButtonProps = {
  // 只能传这三种颜色
  color: 'primary' | 'secondary' | 'danger';
  children: React.ReactNode;
};

const Button = ({ color, children }: ButtonProps) => {
  return <button className={`btn-${color}`}>{children}</button>;
};

// <Button color="red">Click</Button> // ❌ 报错！"red" 不是合法值
// <Button color="danger">Click</Button> // ✅
```

---

## 📝 课后作业

新建 `homework-06.ts`：

1. 定义一个类型别名 `Direction`，它只能是 `"up" | "down" | "left" | "right"` 中的一个。
2. 写一个函数 `move`，接收一个参数 `dir`，类型是 `Direction`。
3. 在函数里打印 "Moving [dir]"。
4. 尝试调用 `move("up")` (成功) 和 `move("forward")` (失败)。

**运行：** `npx tsx homework-06.ts`

---
**下节课预告：** 泛型是什么鬼？TS 最难但也最强大的功能即将登场，别怕，我会用最通俗的例子教会你！👻
