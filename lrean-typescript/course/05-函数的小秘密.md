# 05-函数的小秘密 🕵️‍♂️

## 👋 嗨！
函数 (Function) 是代码世界的搬运工。在 TS 里，我们要给这个搬运工立规矩：
1. **进什么货？** (参数类型)
2. **出什么货？** (返回值类型)

---

## 🚧 1. 基础写法

给参数加上类型，给返回值加上类型。

```typescript
// (x: number, y: number): number
// 参数 x 必须是数字，y 必须是数字，返回值也必须是数字
function add(x: number, y: number): number {
  return x + y;
}

add(1, 2); // ✅
// add("1", 2); // ❌ 参数错了
```

**自动推断返回值**：
TS 通常能根据你的 `return` 语句推断出返回值类型，所以 `: number` 经常可以省略。但为了代码清晰，建议写上。

---

## 🎁 2. 可选参数与默认参数

### 可选参数 (?)
和接口一样，函数参数也可以是可选的。**注意：可选参数必须放在最后！**

```typescript
function sayHello(name: string, age?: number): void {
  if (age) {
    console.log(`Hi ${name}, you are ${age}`);
  } else {
    console.log(`Hi ${name}`);
  }
}

sayHello("Trae"); // ✅
sayHello("Trae", 18); // ✅
```

### 默认参数 (=)
ES6 的特性，TS 完美支持。

```typescript
// 如果不传 msg，默认是 "Hello"
function greet(name: string, msg: string = "Hello"): string {
  return `${msg}, ${name}!`;
}

greet("Alice"); // "Hello, Alice!"
greet("Bob", "Hi"); // "Hi, Bob!"
```

---

## 🏹 3. 箭头函数

箭头函数的类型注解写起来稍微有点长，但原理一样。

```typescript
// 简单写法（推荐）
const multiple = (a: number, b: number): number => {
  return a * b;
};

// 复杂写法（定义函数类型，适合高阶用法，了解即可）
// (a: number, b: number) => number  这一长串是类型
const divide: (a: number, b: number) => number = (a, b) => {
  return a / b;
};
```

---

## 🖼️ Vue 和 React 场景

### Vue (Event Handler)
处理 DOM 事件时，我们需要知道事件对象 `e` 的类型。

```vue
<script setup lang="ts">
// Event 是 DOM 标准类型，TS 内置了
const handleClick = (event: Event) => {
  // 断言 target 是一个 input 元素（进阶知识，先照抄）
  const target = event.target as HTMLInputElement;
  console.log(target.value);
};
</script>

<template>
  <input @input="handleClick" />
</template>
```

### React (Event Handler)
React 的事件类型有点特殊，是 `React.ChangeEvent` 等。

```tsx
import { ChangeEvent } from 'react';

const InputComponent = () => {
  // 专门针对 Input 的 Change 事件
  const handleChange = (e: ChangeEvent<HTMLInputElement>) => {
    console.log(e.target.value);
  };

  return <input onChange={handleChange} />;
};
```
*别怕这些长长的类型，VS Code 鼠标悬停在 `onChange` 上通常会提示你该用什么类型。*

---

## 📝 课后作业

新建 `homework-05.ts`：

1. 写一个函数 `calcArea` (计算面积)，接收 `width` (数字) 和 `height` (数字)，返回它们的乘积。
2. 写一个函数 `getProfile`，接收 `username` (必填) 和 `avatarUrl` (选填)。如果没传 `avatarUrl`，返回 `"User: [username], Avatar: default.png"`，否则返回传入的 URL。
3. 尝试调用它们，看看 TS 怎么保护你的调用不传错参数。

**运行：** `npx tsx homework-05.ts`

---
**下节课预告：** 联合类型与类型别名！如果一个变量既可以是数字也可以是字符串，该怎么办？🤔
