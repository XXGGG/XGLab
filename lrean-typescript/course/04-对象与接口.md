# 04-对象与接口 (Interface) 🏷️

## 👋 嗨！
在 JS 中，对象 (Object) 是我们最常用的数据结构。
在 TS 中，我们需要给对象穿上一套“制服”，规定它必须有哪些属性，每个属性是什么类型。

这套制服，我们通常用 **接口 (Interface)** 来制作。

---

## 👕 1. 什么是 Interface？

Interface 就像是一个**合同**或**模具**。它规定了对象长什么样。

```typescript
// 定义一个接口：User
// 习惯上首字母大写
interface User {
  name: string;
  age: number;
}

// 使用这个接口
const player: User = {
  name: "Trae",
  age: 18
};

// ❌ 报错：少写了 age
// const player2: User = { name: "Bad" }; 

// ❌ 报错：多写了 gender
// const player3: User = { name: "Bad", age: 10, gender: "male" };
```
**重点**：一旦定义了接口，你的对象就必须**严格遵守**，不能多也不能少。

---

## 🎩 2. 灵活一点：可选与只读

### 可选属性 (Optional)
有时候某个属性可有可无，比如“简介”。我们在属性名后面加个 `?`。

```typescript
interface Student {
  name: string;
  score: number;
  bio?: string; // 👈 加上问号，表示这个属性是可选的
}

const s1: Student = { name: "Alice", score: 99 }; // ✅ 没写 bio 也行
const s2: Student = { name: "Bob", score: 80, bio: "I love TS" }; // ✅ 写了也行
```

### 只读属性 (Readonly)
有些属性一旦创建就不能改，比如 `id`。我们在前面加 `readonly`。

```typescript
interface Product {
  readonly id: number; // 👈 只读
  title: string;
}

const book: Product = { id: 1, title: "TS 指南" };

book.title = "TS 高级指南"; // ✅ 可以改
// book.id = 2; // ❌ 报错！id 是只读的，不能改！
```

---

## 🖼️ Vue 和 React 场景

这是开发中最最最常用的场景：定义 API 返回的数据类型。

假设后端给你返回了这样一个 JSON：
```json
{
  "code": 200,
  "data": {
    "userId": 101,
    "username": "admin",
    "token": "abcdefg..."
  }
}
```

### Vue / React 通用写法
我们通常会专门建一个 `types.ts` 文件来存放这些接口：

```typescript
// types.ts

// 定义用户信息
export interface UserInfo {
  userId: number;
  username: string;
  avatar?: string; // 可能没有头像
}

// 定义 API 响应结构
export interface ApiResponse {
  code: number;
  msg?: string;
  data: UserInfo; // 👈 接口里可以套接口！
}
```

然后在组件里使用：
```typescript
const currentUser = ref<UserInfo | null>(null);
```

---

## 📝 课后作业

新建 `homework-04.ts`：

1. 定义一个接口 `Car` (汽车)。
2. 它包含：
   - `brand` (品牌): string, 只读。
   - `price` (价格): number。
   - `color` (颜色): string, 可选。
3. 创建一个变量 `myCar` 使用这个接口，尝试赋值。
4. 尝试修改 `myCar.brand`，观察报错。
5. 尝试创建一个没有 `color` 的车，看看行不行。

**运行：** `npx tsx homework-04.ts`

---
**下节课预告：** 函数的小秘密！参数能乱传吗？返回值怎么定义？TS 里的函数比你想象的更强大！🕵️
